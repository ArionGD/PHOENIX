## now should we add error templates too in this project's root directory as templates folder which will have different error templates so that when error happen user get to know why its happening. is it needed or should we not add it cause we would need to  know the error to sort in the development pahse.

##